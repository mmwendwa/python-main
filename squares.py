"""from functions import square  # importing functions from a different python module

for i in range(10):
    print(f"The square of {i} is {square(i)}")    """

    # or 

import functions   #import the entire python functions module

for i in range(10):
    print(f"The square of {i} is {functions.square(i)}")